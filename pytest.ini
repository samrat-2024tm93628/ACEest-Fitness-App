[pytest]
testpaths = tests test_app.py
python_files = test_*.py
addopts =
    -ra
    --strict-markers
    --cov=app
    --cov-report=term-missing
    --cov-report=xml:coverage.xml
markers =
    smoke: integration smoke tests that hit a running container (deselect with '-m "not smoke"')
